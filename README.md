# portfolio-attention-agent
Volatility-adjusted portfolio alerting for Binance Agent OS — scores each holding's price move against its own historical volatility instead of a flat threshold, so real signals don't get lost in normal noise.
